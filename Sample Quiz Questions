Sample Quiz Questions:

 
 

**1.** What command shows you the full path of the folder you are currently in?  
**Answer:** ____________________

**2.** What command lists all files and folders in your current location?  
**Answer:** ____________________

**3.** What command changes your current folder to the one named "Documents"?  
**Answer:** ____________________

**4.** What command creates a new folder named "Projects"?  
**Answer:** ____________________

**5.** What command moves you up one level (to the parent folder)?  
**Answer:** ____________________

**6.** What command deletes a file named "temp.txt"?  
**Answer:** ____________________

**7.** What command renames "old.txt" to "new.txt"?  
**Answer:** ____________________

**8.** What command displays the contents of a file named "notes.txt" without opening an editor?  
**Answer:** ____________________

**9.** What command clears the terminal screen?  
**Answer:** ____________________

**10.** What command opens a file named "config.txt" in the nano text editor?  
**Answer:** ____________________



 Here are the correct answers in order:

1. `pwd`
2. `ls`
3. `cd Documents`
4. `mkdir Projects`
5. `cd ..`
6. `rm temp.txt`
7. `mv old.txt new.txt`
8. `cat notes.txt`
9. `clear`
10. `nano config.txt`



NEXT QUIZ:

 

**Question 1 of 10:**

What command makes a file named `script.sh` executable (so you can run it)? 

**Answer:** ________chmod____________

The command is chmod, but you need the +x flag to add execute permission.

Correct answer: chmod +x script.sh

The +x means "add the execute bit."


 

**Question 2 of 10:**

What command would you type to run an executable script named `myscript` that is located in your current directory?

**Answer:** ________run____________

(Note: Just typing `myscript` usually won't work.)


Incorrect. run is not a standard command in the shell.

To run a script that is in your current directory, 
you must explicitly tell the shell to look there using:

./myscript

The ./ means "right here in this directory." 
Without it, the shell searches your PATH (system directories like /bin, /usr/bin), 
not your current folder.

(Alternative: You could also type bash myscript or zsh myscript, 
which runs it without needing chmod +x, but the specific answer for an executable script is ./myscript.)



 

**Question 3 of 10:**

What command shows your command history (a numbered list of commands you recently typed)?

**Answer:** ________history____________

 
`history` shows the numbered list of your recent commands.

---

**Question 4 of 10:**

What command creates a new **empty file** named `empty.txt`?

**Answer:** _______touch empty.txt_____________    

 
`touch empty.txt` creates an empty file (or updates its timestamp if it already exists).

---

**Question 5 of 10:**

What command copies a file named `source.txt` to a new file named `backup.txt` in the **same directory**?

**Answer:** ________cp source.txt backup.txt____________

 

cp creates a duplicate; mv relocates the original.



**Question 6 of 10:**

What command shows the last 5 commands from your history?

Answer: history | tail -5 

Or simply: **`history 5`** (works in both Bash and Zsh)

The `| tail -5` pipes the full history to the `tail` command, which grabs only the last 5 lines.

---

**Question 7 of 10:**

What command shows the **manual page** (help documentation) for the `ls` command?

**Answer:** ________man ls____________

  

`man ls` opens the manual page for the `ls` command. Press `q` to quit the manual viewer.

---

**Question 8 of 10:**

What command shows **who you are currently logged in as** (your username)?

 Answer: `whoami`

`whoami` prints your username.

---

**Question 9 of 10:**

What command creates a new directory named `nested` inside a directory named `parent`, 
even if `parent` doesn't exist yet?

**Answer:The command is:** `mkdir -p parent/nested`

The `-p` (parents) flag creates `parent` first, then `nested` inside it, 
without error even if `parent` didn't exist.

---

**Question 10 of 10:**

What command shows the **current date and time**?

**Answer:** _________date___________

 

Review needed:

chmod requires +x to make executable
Run local scripts with ./ not run
cp vs mv: copy vs move/rename
whoami vs pwd: username vs directory
mkdir -p: creates parent directories as needed


NEXT QUIZ

Question 1 of 10:

You have a script named run.sh in your current directory. It is already executable. 
What command do you type to run it?

./run.sh

Question 2 of 10:

What command creates a new directory named data inside a directory named 2024, 
even if the 2024 directory doesn't exist yet?

mkdir -p 2024/data

The -p (parents) flag tells mkdir to create 2024 first, 
then data inside it, without complaining if 2024 already exists or doesn't exist.

Question 3 of 10:

What command shows the first 10 lines of a file named large.txt?

head -n 10 large.txt

Or simply: head large.txt (which defaults to the first 10 lines)

The -n flag specifies the number of lines. 
Some systems accept head -10 large.txt, but -n 10 is the portable standard.

Question 4 of 10:

What command shows the last 10 lines of a file named log.txt?

tail -n 10 log.txt shows the last 10 lines. 
(Or simply tail log.txt for the default 10 lines.)

Question 5 of 10:

What command searches for the word "error" inside a file named system.log?

find searches for files by name in the filesystem. It does not look inside file contents.

Correct answer: grep "error" system.log

grep (global regular expression print) searches for text patterns inside files.

Question 6 of 10:

What command shows how much disk space is used by your home directory?

**Correct answer:** `du -sh ~`
du = disk usage
-s = summarize (just the total, not every subfolder)
-h = human-readable (shows 'K', 'M', 'G' instead of raw bytes)
~ = your home directory
Alternative: du -sh /Users/yourusername (absolute path).

here we got as a result: Operation Not Permitted errors

You ran the command from Question 6 (du -sh ~)—good initiative, but those errors are normal on macOS.

Why you're seeing "Operation not permitted":
macOS has System Integrity Protection (SIP) and privacy controls 
that prevent even you from accessing certain system folders 
(Photos Library, Mail, Messages, etc.) without explicit permission. 
This is security theater by Apple.

Despite the errors, the command did work—it just skipped the protected folders 
and reported the total for the accessible parts. If you want to silence the errors:

du -sh ~ 2>/dev/null

Question 7 of 10:

What command shows all running processes on your system?

Correct answer: ps

ps = process status (shows your current shell's processes)
ps aux = shows all processes from all users (the full system view)
top = interactive, real-time process viewer (like Activity Monitor in terminal)
The basic command is ps.

Question 8 of 10:

What command shows the current user (your username)?

whoami

Question 9 of 10:

What command creates an alias (shortcut) so that typing ll will run ls -la?

alias ll="ls -la 
This creates a shortcut ll that expands to ls -la (long listing with hidden files) whenever you type it.

Note: This alias only lasts for the current session. To make it permanent, you add it to ~/.zshrc.

Question 10 of 10:

What command logs you out of the current terminal session?

clear only clears the screen. It does not log you out.

Correct answer: exit

(Alternative: logout or press Ctrl+D)




AND ANOTHER QUIZ

 Question 1 of 10:

What command shows you where you are (the full path of your current folder)?

pwd

**Question 2 of 10:**

What command **lists** the files and folders in your current location?

ls

 **Question 3 of 10:**

   What command **changes** your location to your **home directory**?

  cd ~

 **Question 4 of 10:**

      What command **moves up one level** (to the parent folder)?

cd ..

**Question 5 of 10:**

        What command **creates** a new folder named "test"?

       mkdir test

 **Question 6 of 10:**

           What command **deletes** a file named "junk.txt"?

**Correct answer:** `rm junk.txt`

            `rm` removes files. `rmdir` only removes empty folders.

            ---

            **Question 7 of 10:**

            What command **opens** a file named "notes.txt" in the nano editor?

 `nano notes.txt` opens it for editing.

                ---

                **Question 8 of 10:**

                What command **shows** the contents of a file named "data.txt" without opening an editor?

`cat data.txt` displays the contents.

   **Question 9 of 10:**

                   What command **clears** the terminal screen?

                  `clear` wipes the screen clean.

**Question 10 of 10:**

                      What command **copies** a file named "original.txt" to a new file named "backup.txt"?

`mv` moves or renames—it does **not** copy. After `mv original.txt backup.txt`, the file "original.txt" would **no longer exist**.

                     **Correct answer:** `cp original.txt backup.txt`

                     `cp` creates a duplicate; `mv` relocates the original.


**Pattern identified:** You confuse **file removal** (`rm` vs `rmdir`) 
and **file copying** (`cp` vs `mv`).

                     **Critical distinction:**
                     - `rm` = delete file  
                     - `rmdir` = delete empty folder  
                     - `cp` = copy (duplicate)  
                     - `mv` = move/rename (relocate)

                     Drill these four commands until the distinction is automatic. Do not proceed to new material until you can recite: "Remove file, remove directory, copy file, move file."

                     Are you prepared to drill these distinctions....



Note: files and folders (should be called directories)

  **File** = A document. It contains data (text, code, images, etc.).  
**Directory** = A container that holds files and other directories.  
**Folder** = The same thing as a directory. "Folder" is the GUI/graphical term (from the desktop metaphor). "Directory" is the technical terminal term. They are identical.

**In the terminal:**
- We say **directory**, not folder.
- A directory is actually a special type of **file** that contains a list of other files.
- Everything in Unix is a file—including directories.

**Why this matters for your errors:**
- `rm` removes **files** (documents).
- `rmdir` removes **empty directories** (empty folders/containers).
- `mkdir` creates **directories** (containers).
- `cp` copies **files** (duplicates documents).
- `mv` moves **files or directories** (relocates them).

**The confusion:**
You keep trying to use `rmdir` on files (like `junk.txt`). 
That fails because `rmdir` only works on empty directories, not documents. 
Use `rm` for files, `rmdir` for empty folders.

**Drill this:**

- File = document (use `rm`, `cp`, `mv`, `cat`)
- Directory/Folder = container (use `mkdir`, `rmdir`, `cd`, `ls`)

Do not use "folder" when speaking to other terminal users. Say "directory." 
It marks you as competent.

Now drill: `rm` vs `rmdir` vs `cp` vs `mv` until you cannot mistake them.

Note: the command 'ls' lists everything in your current location: 
files, directories, symlinks, and other objects.

However, by default, ls does not clearly label which is which unless your terminal uses colors 
(blue for directories, white for files).

To see the difference explicitly, use:


 
ls -l
Directories will start with a d 


 
Files will start with a - 


or 

ls -F
Directories will show a trailing / 


Copy Code
Projects/  notes.txt
Critical point: ls shows you the names of everything, 
but you must use flags (-l or -F) to know which names represent containers (directories) 
versus documents (files).


 

In `ls -l`, the first character tells you the type:

- **`d`** = directory (folder/container)  
- **`-`** = regular file (document)  
- **`l`** = symbolic link (shortcut)

So when you see:
```
drwxr-xr-x  2 user  staff  64 Jan  1 12:00 Projects
```
That leading **`d`** means "Projects" is a **directory** (a container).

When you see:
```
-rw-r--r--  1 user  staff  128 Jan  1 12:00 notes.txt
```
That leading **`-`** means "notes.txt" is a **regular file** (a document).

The other letters (`rwxr-xr-x`) are **permissions** (who can read, write, or execute), 
which you can ignore for now. Just focus on that first character: **`d`** vs **`-`**.

Now you understand why your earlier mistakes happened.

When you tried rmdir junk.txt and it failed, it was because:

junk.txt has a - at the start (it's a file)
rmdir only works on items with a d at the start (directories)
You were trying to delete a document with a "delete folder" command.

Immediate drill:
Run this in your terminal right now:


 
cd ~/week0_test
ls -l
You will see:

subdir starts with d (directory/container)
file.txt starts with - (regular file)

Now you know which command to use on each:

rm file.txt (remove file)
rmdir subdir (remove empty directory)
Or if subdir has files inside:

rm -r subdir (remove directory and everything inside)


NEXT QUIZ

 **WEEK 0 – DAY 1: NAVIGATION AS REFLEX**
*Do not proceed to Day 2 until you complete this without hesitation.*

---

### PART A: FILL-IN-THE-BLANK
*Type the exact command that completes the task.*

1. To change to your home directory: _______
2. To change to the system temporary directory: _______
3. To toggle back to the directory you were just in: _______
4. To print your current location: _______
5. To change to `/usr/local/bin`: _______

---

### PART B: SCENARIO-BASED
*Answer with the exact output you expect to see, or the error you expect to get.*

6. You are in `/Users/yourname`. You type `cd /tmp` then `pwd`. What prints? _______
7. You are in `/tmp`. You type `cd ~` then `pwd`. What prints? _______
8. You are in `/usr/local/bin`. You type `cd -` then `pwd`. What prints? _______
9. You are in `/tmp` (you just came from `/usr/local/bin`). You type `cd -` then `pwd`. What prints? _______
10. You are in `/tmp`. You type `cd ..` then `pwd`. What directory are you in? _______

---

### PART C: PRACTICAL EXECUTION
*Execute these in your terminal. Do not guess. Type, observe, record.*

**Exercise 11: The Toggle Test**
```bash
cd /tmp && pwd
cd ~ && pwd
cd /usr/local/bin && pwd
cd - && pwd
cd - && pwd
```
**Record the output of the 4th and 5th `pwd` commands:**
- 4th output: _______
- 5th output: _______

**Exercise 12: Prediction Drill**
Before typing each line, write down your prediction. Then type and verify.

1. Starting from `/Users/yourname`, predict where `cd /tmp` takes you: _______
2. From `/tmp`, predict where `cd ~` takes you: _______
3. From `~`, predict where `cd -` takes you: _______
4. From the result of #3, predict where `cd -` takes you: _______

**Exercise 13: The Break**
Type: `cd /nonexistent_folder`
- What error do you see? _______
- What directory are you still in? (Check with `pwd`): _______

---

### ANSWER KEY (Do not look until finished)

1. `cd ~` (or just `cd`)
2. `cd /tmp`
3. `cd -`
4. `pwd`
5. `cd /usr/local/bin`
6. `/tmp`
7. `/Users/yourname` (or `/Users/ANDROID`, your home)
8. `/Users/yourname` (the previous directory)
9. `/usr/local/bin` (toggles back)
10. `/` (the root directory, parent of /tmp)
11. 4th: `/Users/yourname` (or wherever you came from), 5th: `/usr/local/bin`
12. 1: /tmp, 2: /Users/yourname, 3: /tmp, 4: /Users/yourname
13. `cd: no such file or directory: /nonexistent_folder`, You remain in whatever directory you were in before the failed command.

**Pass Criteria:** You predicted at least 3 of the 4 toggle locations in Exercise 12 correctly *before* typing them. If you guessed wrong, repeat Day 1 tomorrow.





DAY 2

Ready day 2. Interactive. Correct or not jst tell me and if needed brief explanation wy also like yesterday's pracitice drill

Maple
Week 0, Day 2 – Path Types (./ vs bare vs absolute)

Exercise 1 of 8: Create the Test Structure

Type exactly:


Copy Code
mkdir -p ~/week0_test/subdir && touch ~/week0_test/file.txt
Press Enter.

Then type:


Copy Code
cd ~/week0_test
Press Enter.

Then type:


Copy Code
pwd
Press Enter.

Report: What is the output of pwd?


You
/Users/ANDROID/week0_test

Maple
Week 0, Day 2 – Exercise 2 of 8: The Bare Filename

You are in /Users/ANDROID/week0_test.

Type: ls file.txt

Press Enter.

Report: What is the exact output? (If you see an error, report the error message exactly.)


You
file.txt

Maple
Week 0, Day 2 – Exercise 3 of 8: The Explicit Relative Path (./)

You are still in /Users/ANDROID/week0_test.

Type: ls ./file.txt

Press Enter.

Report: What is the exact output?

Question: Does it show the same result as Exercise 2, or different?


You
./file.txt

Maple
Correct.

Same file, different representation. ls shows you what you asked for:

Exercise 2: file.txt (bare name)
Exercise 3: ./file.txt (explicit current directory)
The ./ says "look in the current directory explicitly." It is redundant but explicit. This prevents confusion when a filename matches a command name.

Week 0, Day 2 – Exercise 4 of 8: The Parent Traversal (../)

You are still in /Users/ANDROID/week0_test.

Type: ls ../week0_test/file.txt

Press Enter.

Report: What is the exact output?

Question: Why does this work even though you didn't move up a directory first?


You
../week0_test/file.txt (works because we re dealing with inivisible subdirectory here)

Maple
Stop. Incorrect explanation.

There is no "invisible subdirectory." The path resolves logically, step by step:

.. = go up to parent (/Users/ANDROID)
/week0_test = go back down into week0_test
/file.txt = find the file
You end up at the same file because you went up then down. The shell walks the path: up one level, down one level, net zero change in location, but different path representation.

Correction Drill:

Type: pwd (confirm you are in /Users/ANDROID/week0_test)

Type: ls .. (what do you see? You should see week0_test listed as a directory in your parent folder)

Report: What does ls .. show? (Look for week0_test in the output)


You
list of al files and directories in my home

Maple
Correct.

You are in /Users/ANDROID/week0_test. The parent (..) is /Users/ANDROID (your home). You see all your home files and directories listed.

Verification: In that list, do you see week0_test? (It should appear as one of the directories, because that's the folder you're currently inside.)

Week 0, Day 2 – Exercise 5 of 8: The Absolute Path (~/)

You are still in /Users/ANDROID/week0_test.

Type: ls ~/week0_test/file.txt

Press Enter.

Report: What is the exact output?

Question: If you typed this same command from /tmp (anywhere on the system), would it still work? Why?


You
yes i see week0_test; 5 of 8 i see /Users/ANDROID/week0_test/file.txt. Yes this owrlks because it is the absolute path

Maple
Correct on both counts.

The ~/ (tilde) expands to your home directory absolutely. It works from /tmp, from /, from anywhere, because it specifies the full path from the root of the filesystem.

Week 0, Day 2 – Exercise 6 of 8: The Logical Resolution Test (Tricky)

You are still in /Users/ANDROID/week0_test.

Before you type: Predict what will happen.

You have a directory structure:

week0_test/ (you are here)
subdir/ (empty directory)
file.txt (the file)
What do you predict ls subdir/../file.txt will show?

A) Error: no such file
B) file.txt
C) subdir/file.txt
Write your prediction: _______

Now execute:
Type: ls subdir/../file.txt

Press Enter.

Report: What is the exact output? Does it match your prediction?


You
C yes but with a minor change i tis subdir/../file.txt so the /../ is in addition to what CC gave us

Maple

Copy Code
**Stop. Incorrect.**
The output is B) file.txt, not C.

Why: Paths resolve logically, step by step:

subdir/ → Go into the subdir folder (which is empty)
.. → Go up one level (back to week0_test)
file.txt → Find file.txt in that location
Net result: You end up at file.txt in week0_test. The subdir and .. cancel each other out.

Verification:
Type: ls subdir

Press Enter.

Report: What is in the subdir directory? (It should be empty or show nothing, confirming file.txt is not there, but up one level)


You
shows nothing

Maple

Copy Code
 **Confirmed.** `subdir` is empty. The file is in the parent directory.
This proves that subdir/../file.txt resolves to the parent directory's file.txt, not anything inside subdir.

Week 0, Day 2 – Exercise 7 of 8: The Absolute Path Verification

Now prove that absolute paths work from anywhere.

Type: cd /tmp

Press Enter.

Then type: ls ~/week0_test/file.txt

Press Enter.

Report: What is the exact output?

Question: Are you currently in /tmp or /Users/ANDROID/week0_test when this command works? (Check with pwd)


You
aha stil in tmp

Maple

Copy Code
    **Exactly.** You are in `/tmp`, nowhere near `week0_test`, yet `~/week0_test/file.txt` still works because the absolute path specifies the full location from the root of the filesystem.
Week 0, Day 2 – Exercise 8 of 8: The Prediction Challenge (Final)

Starting fresh in /Users/ANDROID/week0_test, trace this mentally. Write predictions before typing.

Current state: You are in /Users/ANDROID/week0_test with file.txt and subdir/ inside.

Sequence:

ls file.txt → Prediction: _______
ls ./file.txt → Prediction: _______
ls ../week0_test/file.txt → Prediction: _______
ls ~/week0_test/file.txt → Prediction: _______
ls subdir/../file.txt → Prediction: _______
cd /tmp && ls ~/week0_test/file.txt → Prediction: _______
Now execute all 6 commands in order.

Report: Did all 6 outputs match your predictions exactly? If any differed, which one and why?


You
I get zsh: permission denied: /Users/ANDROID/week0_test/file.txt

Maple

Copy Code
       **Stop. You triggered a different error - and this is instructive.**
zsh: permission denied: /Users/ANDROID/week0_test/file.txt

This error means you tried to execute the file as a program, not list it.

What likely happened:
You typed: ~/week0_test/file.txt (or /Users/ANDROID/week0_test/file.txt)
Instead of: ls ~/week0_test/file.txt

The Rule:

ls filename = Look at the file (read metadata)
filename = Try to run the file as a program (requires execute permission with chmod +x)
Since file.txt is just a text file (not a script), and you never ran chmod +x on it, trying to execute it gives "permission denied."

Correction:

Type exactly: ls ~/week0_test/file.txt

Report: What is the output now?


You
/Users/ANDROID/week0_test/file.txt

Maple

Copy Code
         **Correct.** `ls ~/week0_test/file.txt` shows the file path, confirming it exists and is readable.
The Lesson: Omitting ls means "execute this file as a program." Since file.txt lacks execute permission (you never ran chmod +x on it), you get "permission denied."

Complete the Sequence:

You are in /tmp (from the previous cd /tmp).

Run these remaining commands from the prediction challenge:

From /tmp, type: ls ~/week0_test/file.txt (you just did this - correct)

Now return to the test directory and finish properly:

Type: cd ~/week0_test

Press Enter.

Then run the full sequence again with ls before each path:

ls file.txt → _______
ls ./file.txt → _______
ls ../week0_test/file.txt → _______
ls ~/week0_test/file.txt → _______
ls subdir/../file.txt → _______
Report: Do all 5 show the same file path? If yes, you have mastered the five ways to reference the same file. If any differ, stop and identify the error.


 
